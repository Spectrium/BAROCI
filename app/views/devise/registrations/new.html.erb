<!-- appel css 
 -->
<link rel="stylesheet" href="/css/signup.css" />
<!-- html body  --> 

<div class="box-signup">
  <div class="sign-up">
    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %> <%= render "devise/shared/error_messages", resource: resource %>
    <h1 class="text-center mb-3">Inscription</h1>
    <div class="d-flex justify-content-around">
      <div class="mr-1 mb-2">
        <label for="nom-inscri">Nom :</label>
        <%= f.text_field :full_name, autofocus: true, id:'nom-inscri', class: 'form-control text-center neo-form', autocomplete: "Nom Complet",placeholder:"Nom", :required => true,:minlength => 3 %>
      </div>
      <div class="ml-1">
        <label for="email-inscri">Email :</label>
        <%= f.email_field :email, autofocus: true, id:'email-inscri',class: 'form-control text-center neo-form', autocomplete: "email",placeholder:" adressee-mail", :required => true %>
      </div>
    </div>
    <div class="d-flex justify-content-around">
      <div class="mr-1 mb-2">
        <label for="password-inscri">Mot de passe :</label>
        <%= f.password_field :password, class: 'form-control text-center neo-form', id:'password-inscri', autocomplete: "new-password",placeholder:"Mots de passe", :required => true %>
      </div>
      <div class="ml-1">
        <label for="password-inscri-confirm">confirmer le mot de passe :</label>
        <%= f.password_field :password_confirmation, id:'password-inscri-confirm',class: 'form-control text-center neo-form', autocomplete: "new-password",placeholder:"Confirmer mots de passe", :required => true %>
      </div>
    </div>
    
    <div class="text-center">
      <%= f.submit "S'inscrire", class:"btn btn-primary log-submit"%>
      <p class="submit-error" style="color: red;"></p>
    </div>
    <% end %> <%= render "devise/shared/links" %>

  <!-- Flash message  -->
    <div class="flash">
      <div class="flash__icon">
        <i class="icon fa fa-info"></i>
      </div>
      <% if flash.each do |type, msg| %>
      <p class="flash__body font-weight-bold"><%= msg %></p>
      <% end.empty? %>
      <p class="font-weight-bold">Remplir les formulaires pour s'inscrire!</p>
      <% end %>
    </div>
  </div>

  <div class="box-signup-image">
    <img src="/images/logo.webp" alt="image mada" />
  </div>
</div>

  
  <!-- Verification mots de passe  -->
<script>
  $('#password-inscri-confirm').on('input', function () {
    if ($('#password-inscri').val() != $(this).val()) {
      $('.log-submit').prop('disabled', true);
    } else {
      $('.log-submit').prop('disabled', false);
    }
  });
  $('#password-inscri-confirm').focusout(function () {
    if ($('#password-inscri').val() != $('#password-inscri-confirm').val()) {      
      alert( "Mots de passe et la confirmation ne sont pas égaux.");
    } else {
      $('.submit-error').hide();
    }
  });
</script>
