
<div class="container">
    <div class="row">
        <div class="col-6">
            <div class="card">
                <div class="card-body">
                    <div class="row">
                        <div class="col-12 col-lg-8 col-md-6">
                            <h3 class="mb-0 text-truncated"><%= @user.full_name  %></h3>
                            <p class="lead"><%= @user.age  %><br>
                                            <%= @user.email %><br></p>
                            <p>
                                I love to read, hang out with friends, watch football, listen to music, and learn new things.
                            </p>
                            <hr>
                            <p>Changer d'avatar ?</p>
                              <%= form_tag user_portraits_path(@user), multipart: true do %>
                              <%= file_field_tag :portrait %>
                              <%= submit_tag "mettre à jour" %>
                              <% end %>

                        </div>
                        <div class="col-12 col-lg-4 col-md-6 text-center">
                        <%if @user.portrait.attached?%>
                          <%= image_tag @user.portrait, class:"imagauto", alt: 'avatar' %>
                          <%else%>
<img src="https://robohash.org/68.186.255.198.png" alt="" class="imagauto">                          <%end%>
                            <br>
                            <% if @user == current_user %>
	<%= link_to 'modifier profil', edit_user_path(@user.id)  %> <br>
<% end %>

<% if current_user.is_admins == true %>
	<%= link_to 'Supprimer ce compte', user_path(@user.id) , method: 'delete', data:{confirm:'Etes-vous sure bordel de merde'} %>
<% end %>
                        </div>
                    </div>
                    <!--/row-->
                </div>
                <!--/card-block-->
            </div>
        </div>
    </div>
</div>