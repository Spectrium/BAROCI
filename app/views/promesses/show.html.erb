
<div class="card" style="width: 18rem;">
<div id="carouselExampleFade" class="carousel slide carousel-fade " data-ride="carousel">
      <div class="carousel-inner carousel-display">
        <div class="carousel-item active">
          <%if @promesse.pictures.attached?%>
            <%= image_tag (@promesse.pictures[0]), class:"img-display carousel-display card-img-top proimg",alt: '#{@car.model.name} #{@car.model.name}' %>
          <%else%>
          tsisy
            <%
=begin%>
 <%= image_tag (@car.image_url),class:"img-display carousel-display",alt: '#{@car.model.name} #{@car.model.name}' %> 
<%
=end%>
          <%end%>
        </div>
        <% for i in 1..(@promesse.pictures.length-1)%>
        <div class="carousel-item">
          <%if @promesse.pictures.attached?%>
            <%= image_tag (@promesse.pictures[i]), class:"img-display carousel-display card-img-top proimg",alt: '#{@car.model.name} #{@car.model.name}' %>
          <%else%>
          tsisy
            <%
=begin%>
 <%= image_tag (@car.image_url),class:"img-display carousel-display",alt: '#{@car.model.name} #{@car.model.name}' %> 
<%
=end%>
          <%end%>
        </div>
        <%end%>
          <a class="carousel-control-prev" href="#carouselExampleFade" role="button" data-slide="prev">
            <span class="carousel-control-prev-icon" aria-hidden="true"></span>
            <span class="sr-only">Previous</span>
          </a>
          <a class="carousel-control-next" href="#carouselExampleFade" role="button" data-slide="next">
            <span class="carousel-control-next-icon" aria-hidden="true"></span>
            <span class="sr-only">Next</span>
          </a>
      </div>
  <div class="card-body">
    <p class="card-text">Some quick example text to build on the card title and make up the bulk of the card's content.</p>
  </div>
  <div>
    <%= form_tag promess_pictures_path(@promesse), multipart: true do %>
    <%= file_field_tag :picture %>
    <%= submit_tag "mettre à jour" %>
    <% end %>
  </div>
</div>

